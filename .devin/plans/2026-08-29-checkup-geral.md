# Plano de planos: checkup geral v2.8.0

**Objetivo:** corrigir todas as inconsistencias documentais encontradas no checkup de 2026-08-29 em um unico PR.

## Issues encontradas

### P1 — README.md (4 issues)

| # | Local | Problema | Fix |
|---|---|---|---|
| 1 | Linha 62, tree | `19 consolidated rules` | Mudar para `20` |
| 2 | Linha 44, Mermaid | `13 Python hooks` | Mudar para `17 Python scripts` |
| 3 | Linhas 120-141, tabela hooks | Faltam `context-pressure.py` (PostToolUse) e `SessionEnd`/`memory-stop.py` | Adicionar 2 linhas |
| 4 | Linha 69, tree | `credentials.toml` listado mas nao existe no repo | Adicionar comentario `(generated by export -NoMask)` |

### P2 — docs/TOOLS-MAP.md (3 issues)

| # | Local | Problema | Fix |
|---|---|---|---|
| 5 | Linhas 91-93 | `SessionEnd` listado como "nao utilizado" mas e usado com `memory-stop.py` | Mover para tabela ativa e adicionar linha |
| 6 | Tabela hooks | Falta `context-pressure.py` (PostToolUse) | Adicionar linha |
| 7 | Linha 103 | `19 regras` | Mudar para `20` |

### P3 — docs/SKILL-TIERS.md (1 issue)

| # | Local | Problema | Fix |
|---|---|---|---|
| 8 | Tabela de skills | `youtube-fetcher` ausente | Adicionar na tier apropriada |

### P4 — CONTRIBUTING.md (1 issue)

| # | Local | Problema | Fix |
|---|---|---|---|
| 9 | Linha 61 | `exit 1` para block | Mudar para `exit 2` (contrato Devin CLI) |

### P5 — SECURITY.md (2 issues)

| # | Local | Problema | Fix |
|---|---|---|---|
| 10 | Linha 25 | `post-compaction-reminder.py` (removido v2.4.0) | Mudar para `constraint-pinning.py` |
| 11 | Linha 27 | `refine` skill (nao existe mais) | Mudar para `primeagent-reference` Refine mode |

### P6 — hooks.v1.json (template projeto) desatualizado (1 issue)

| # | Local | Problema | Fix |
|---|---|---|---|
| 12 | `.devin/hooks.v1.json` e `hooks.v1.json` | Faltam hooks: `memory-post-exec.py`, `memory-post-edit.py` (PostToolUse write|edit), `memory-retrieval.py`, `memory-stop.py` (Stop), `constraint-pinning.py` (PostCompaction, UserPromptSubmit, SessionStart), `context-pressure.py`, `silent-error-review.py`, `validate-mermaid.py`, `validate-tool-args.py`, `check-ai-signature.py` (Stop), `refine-review-prompt.py`, `behavioral-nudge.py` (UserPromptSubmit) | Sincronizar com config.json (sem os paths absolutos, sem PermissionRequest vazio) |

### P7 — validate-skill-format.py warnings (12 skills)

| # | Local | Problema | Fix |
|---|---|---|---|
| 13 | 12 skills com score 95 | `description should start with 'Use when'` | Ajustar frontmatter de: `a11y-audit`, `api-design`, `cost-optimization`, `database`, `deploy`, `docker`, `e2e-testing`, `i18n`, `impeccable`, `legacy-refactor`, `performance`, `security-audit` |

## Ordem de execucao

1. **Sub-plano A — Documentacao** (issues 1-11): correcoes textuais em README, TOOLS-MAP, SKILL-TIERS, CONTRIBUTING, SECURITY.
2. **Sub-plano B — hooks.v1.json** (issue 12): sincronizar template de projeto com config.json.
3. **Sub-plano C — Skill descriptions** (issue 13): ajustar frontmatter dos 12 skills para score 100.

Cada sub-plano pode ser um commit separado no mesmo PR.

## Contrato de execucao

- [ ] Nao modificar `tests/held-out/`.
- [ ] Executar `python audit.py` apos cada sub-plano.
- [ ] Executar `python -m pytest tests/ -q` apos cada sub-plano.
- [ ] Executar `python scripts/validate-skill-format.py` apos sub-plano C.
- [ ] Criar PR unico com os 3 commits.
- [ ] Nao alterar comportamento de nenhum script.

## Criterio de conclusao

- [ ] Todos os 13 issues corrigidos.
- [ ] `audit.py` 0 erros, 0 warnings.
- [ ] `validate-skill-format.py` 148/148 PASS (0 WARN).
- [ ] 229 testes passando.
- [ ] Held-out 135 passando.
- [ ] CI verde.
